var latlong;
function setloc(loc){
	switch{
		case loc == plymouth:
      latlong = plymouth;
      break;
    case loc == manchester:
      latlong = manchester;
      break;
    case loc == london:
      latlong = london;
      break;
	}
}
